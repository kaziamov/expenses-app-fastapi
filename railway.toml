[build]
dockerfilePath = "Dockerfile.backend"

[deploy]
startCommand = "alembic upgrade head && uvicorn expenses_app:fastapi_app --host 0.0.0.0 --port 8000 --log-config=log_config.ini"
restartPolicyType = "never"